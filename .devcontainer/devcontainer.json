{
  "name": "Laravel + Next.js App",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "frontend",
  "workspaceFolder": "/app",
  "forwardPorts": [3000, 8000, 5432, 8025],
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "postCreateCommand": "composer install && npm install && npm run dev && echo 'Codespace ready!'",
  "remoteUser": "vscode"
}